# ESM3ΔG-SKEMPIv2 — experiment record  (created 2026-07-01; status: RUNNING)

> **REDO**:第一次 run(2026-06-30)跑在**已污染并删除的** `/home/guoj0f/repos/esm` 依赖上,结果作废(旧 test per-structure 0.071,不可信)。本次在**干净的共享依赖** `/home/guoj0f/share/esm`(esm 3.3.0)+ `share/hf_cache`(ESM3 权重)上重跑。

## 1. Goal / hypothesis
MGnify 预训练的稳定性模型(ESM3ΔG)+ LoRA 微调,能否迁移到 SKEMPI binding ΔΔG。本任务 = "MGnify(经 ESM3ΔG)+ LoRA,SKEMPI-only"。与 task2(加 Megascale stage)配对对比。

## 2. Design & decision points  (写于提交前)
- **架构(忠实微调 ESM3ΔG)**:ESM3(冻结)+ LoRA r=4 + stability head + SigmoidScaling;`binding_ddG = complex − binder1 − binder2`,`folding_dG` = stability head 逐残基 ΔG 的均值(非特殊 token)。纯 LoRA+head 训练(trunk + VQ-VAE 编码器全冻结,4.14M 可训练参数)。
- **KEY DECISIONS**:
  - **模型版本 = `ESM3dG_weights_augmented_1`**(单成员)。⚠ **用户保留 ensemble/base 的最终决策权**;本次先 augmented_1。
  - 超参:lr 6e-5 / AdamW / wd 0.05 / 15 ep / seed 0 / `--max_batch 4`(OOM 时 M→2→1 重试)/ `--save_freq 5`。
  - 数据:SKEMPI `filtered_skempi.csv`,train 120 complex/3050 mutants,test 81 complex/1491 mutants。
- **对标**:ProteinMPNN(同 test split)per-structure **0.448** / overall 0.531。
- **指标**:test per-structure Spearman(复合物内排序均值)+ overall。

## 3. Run config
- SLURM:a100 ×1(A100-SXM4-80GB),walltime **10h**(据上一次相同 run 实测 6h52m + ~40% buffer;从 20h 下调以改善 backfill/缩短排队),cpus 12,mem 96G。
- sbatch:`ibex-records/ESM3ΔG-SKEMPIv2/sh/esm3dg_skempi_aug1_20260701-145954.sh`。
- **env:`esm3dg`(py3.12 + torch2.6 + esm from `/ibex/user/guoj0f/share/esm`,本任务独立 env,离线从 per-branch wheelhouse 建(见 esm3dg_stab/env_setup/))**;`HF_HOME=/ibex/user/guoj0f/share/hf_cache`,`HF_HUB_OFFLINE=1`。
- 代码:`/ibex/user/guoj0f/StaB-ddG/MGnify-replace/esm3dg_stab/`(clean 重同步)。

## 4. Change log  (LIVE)
- 2026-07-01:依赖从已删的 repos/esm 迁到 share/esm;旧 run 记录已清除;本 plan 写于重跑前。job 未提交(待 env `esm3dg` 重建完成 + Ibex smoke 通过)。

- 2026-07-01(cont.):env 明确为**独立 `esm3dg`**(py3.12,torch2.6+cu124,esm 3.3.0 editable from share/esm;从本地打的 per-branch wheelhouse 离线装 + `--no-build-isolation`)。**不用 esm-backbone**(那是别 task 的 env)。本地 + Ibex a100 smoke 均通过(ESM3ΔG 加载/预测 ΔG 一致、scorer OK)。**首次提交误用 esm-backbone 已取消**,改用 esm3dg 重新提交。

- 2026-07-01(cont.):**task1 (ESM3ΔG→SKEMPI) 提交,job 47936771**(a100-80GB,esm3dg env)。

- 2026-07-01(cont.):job PENDING(等 a100)。据 sacct 上一次相同 run 实测 6h52m,把 walltime 20h→**10h**(`scontrol update` 改 live job + 同步 sbatch 源文件),`--start` 估计从 07-04 提前到 07-03。

## 5. Results  (COMPLETED 2026-07-02;job 47936771,a100,elapsed 6h52m)

**SKEMPI test(81 complexes / 1491 mutants;OOM-skip 6 complexes,M-fallback 兜底):**

| 指标 | task1 (clean) | 旧(作废,污染依赖) | ProteinMPNN baseline |
|---|---|---|---|
| **per-structure Spearman (mean)** | **0.1584** | 0.071 | **0.448** |
| overall Spearman | 0.2763 | — | 0.531 |
| overall Pearson | 0.2615 | — | — |

- train 曲线健康收敛:epoch1 loss4.20/spr0.177 → epoch15 loss0.79/**train_spearman 0.617**(单调,无发散)。
- 预测 csv 留证:`results/eval_esm3dg_skempi_aug1.csv`;adapter:`cache/esm3dg_skempi_aug1.pt`(202 tensors)。

**判读:**
1. **干净依赖把 per-structure 从 0.071 → 0.158(翻倍)** —— 证实此前的污染依赖确实拉低了分数;但清干净后**仍远低于 ProteinMPNN 0.448**,说明低分**不只是依赖问题,存在真实方法性 gap**。
2. **train 0.617 vs test per-structure 0.158** = 明显泛化 gap(train 120 complexes / LoRA 可能过拟合)。
3. **overall 0.276 > per-structure 0.158** = 模型抓到了跨复合物量级,但**复合物内突变排序**弱 —— 而 per-structure(binding ΔΔG 最看重的)正是弱项。

**诊断候选(待与 task2 合并判定后,按"让用户决策"再定,不自行改设计):** 早停(用 ep5/ep10 ckpt 看 test 是否更高)/ SUM 而非 MEAN 聚合逐残基 ΔG(减轻界面信号稀释)/ 检查 StaB 分解 `complex−b1−b2` 的数值噪声。

### 5b. 早停诊断(2026-07-02,job 47954798,eval-only,零代码改动)
eval 训练中途 ckpt(save_freq=5),看早停能否胜过 ep15-final:

| epoch | per-structure | overall Spearman |
|---|---|---|
| ep5 | 0.0379 | 0.3000 |
| ep10 | 0.1294 | 0.3040 |
| **ep15 (final)** | **0.1584** | 0.2763 |

**结论:早停无用。** per-structure **单调上升**(0.038→0.129→0.158),ep15 就是最优——train≫test 的 gap **不是**"过拟合掉分"型(否则早停应更高),而是**真实容量/信号 gap**。overall 在 ep10 见顶(0.304)后微降,与 per-structure 分道扬镳;但主指标(per-structure,对标 ProteinMPNN)取 ep15。**排除早停这条改进路径。** csv 留证:`results/eval_esm3dg_skempi_aug1_ep{5,10}.csv`。

### 5c. ⚠ 口径订正(2026-07-05):本文 per-structure = **≥2 突变**口径,**与 StaB baseline 0.448 的 THRESHOLD=10 口径不一致、不可比**
- StaB 的 per-structure(含 0.448 baseline)用 `baselines/eval_utils.compute_metrics` 的 **THRESHOLD=10**(仅 ≥10 突变复合物)。见 memory [[stabddg-per-interface-threshold10]]。
- 从本实验 ckpt 的 `results/eval_esm3dg_skempi_aug1.csv` **重算 THRESHOLD=10**:per-structure **0.1926**(43/81 复合物 ≥10 mut),overall 0.2763。即本文的 **0.1584 是 ≥2 口径**;对齐 StaB 后为 **0.193**。
- ⚠ 两点:(1) 即便对齐,0.193 仍远 « 0.448;(2) **此 run 本身可能有问题(用户 2026-07-05 指出)+ 有 OOM-skip**,数值仅供参考,勿作硬结论。
